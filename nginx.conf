server {
    listen 80;
    server_name yourdomain.com;   # ← replace with your domain or EC2 public IP

    # Redirect HTTP → HTTPS (uncomment after adding SSL cert)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass         http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    # Optional: block access to internal debug endpoints in production
    location /docs {
        deny all;
    }
    location /redoc {
        deny all;
    }
}

# ── HTTPS block (after running certbot) ──────────────
# server {
#     listen 443 ssl;
#     server_name yourdomain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         ...
#     }
# }
